package com.example.simplenotepad.model

import com.example.simplenotepad.R
import com.example.simplenotepad.databinding.ModelLoadingStateBinding

class LoadingStateModel : ViewBindingKotlinModel<ModelLoadingStateBinding>(R.layout.model_loading_state) {
    override fun ModelLoadingStateBinding.bind() {
        //nothing to do
    }
}